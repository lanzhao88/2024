#!/bin/bash

# [Lan] GPU FS-mode disabled to save storage in Codespaces
# (This script normally unpacks a 55GB GPU disk image into /tmp)
# All GPU full-system setup steps are skipped.

# if [ ! -f /tmp/x86-ubuntu-gpu-ml-isca ]; then
#  cp x86-ubuntu-gpu-ml-isca.gz /tmp
#  gunzip /tmp/x86-ubuntu-gpu-ml-isca.gz
# fi

# ln -s /tmp/x86-ubuntu-gpu-ml-isca .
